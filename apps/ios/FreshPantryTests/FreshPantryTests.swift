import Testing
@testable import FreshPantry

/// Smoke test placeholder. Real coverage (SwiftData models, sync codecs, domain
/// logic) is added per feature as it migrates.
struct FreshPantryTests {
    @Test func appModuleLinks() {
        #expect(Bool(true))
    }
}
